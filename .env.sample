# --- API KEYS ---
BINANCE_API_KEY=YOUR_BINANCE_FUTURES_KEY
BINANCE_API_SECRET=YOUR_BINANCE_FUTURES_SECRET

# --- CORE SETTINGS ---
SYMBOL=BTCUSDT          # Perpetual symbol
GRID_LEVELS=20          # per side
STEP_PCT=0.25           # % step between consecutive levels (e.g., 0.25 = 0.25%)
TP_PCT=0.20             # take-profit distance from entry (%)
ORDER_USDT=20           # notional per entry order (sized to 1.2x effective exposure)
MAX_RANGE_PCT=4.0       # breakout guard: deactivate if price leaves mid ± this %
EFFECTIVE_EXPOSURE=1.2  # emulate 1.2x by sizing orders (while leverage is set to 1x)

# Risk/ops
ISOLATED=true
DRY_RUN=true
